{
  "name": "requestor",
  "version": "1.0.0",
  "description": "Make API request to get auth tokens",
  "main": "server.js",
  "engines": {
      "node": "13.12.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.0"
  }
}
